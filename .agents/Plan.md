# Legacy Plan Pointer

This file is not an active plan and is not a plan template. It exists only to
prevent the legacy `.agents/Plan.md` name from becoming a competing source of
truth.

- Read `.agents/PLANS.md` for planning policy.
- Store every durable planning artifact under `.agents/execplans/`.
- Record tasks, checkpoints, and progress only in the relevant active artifact
  there.
